name: build-macos
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls lib/raylib
      - run: ./libs.sh
      - run: mkdir bin
      - run: ./build.sh
