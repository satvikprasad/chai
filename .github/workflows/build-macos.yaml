name: build-macos
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: egor-tensin/setup-gcc@v1
        with:
          version: latest
          platform: x64a
      - run: ./libs.sh
      - run: mkdir bin
      - run: ./build.sh
