name: build-macos
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
        git submodule update --init --recursive
        ls lib/raylib
        ./libs.sh
        mkdir bin
        ./build.sh
